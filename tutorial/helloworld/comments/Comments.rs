
fn main() {
    // comments all same as other some major languages such as java, C++
    /**
     * this is block comments!
     */
    /*
    Note: The previous column of `*` was entriely for style. There's
    no actual need for it.
     */

    let x = 5 + /* 90 + */ 5;   // warning: use `/* */` for a plain comment. but the tutorial says to do.
    // It strange to me this format.. but I saw in python.
    println!("Is `x` is 10 or 100? x = {}", x);
}
